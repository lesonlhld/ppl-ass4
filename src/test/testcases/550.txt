Function: main
        Body:
        Var: x = 5;
            While (True) Do
                While (x>=0) Do
                    x = x+-2;
                EndWhile.
                If ((x<0)) Then
                Break;
                EndIf.
            EndWhile.
            print(string_of_float(float_to_int(x)));
        EndBody.