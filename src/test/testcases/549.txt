Function: main 
        Body:
            Var: x=20;
            While True Do
                If (x>0) && (x<3) Then 
                printStrLn("oke");
                Break;
                Else printStrLn("Error");
                EndIf.
                x=x-3;
            EndWhile.
        EndBody.